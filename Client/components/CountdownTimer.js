/*import Countdown from 'react-countdown';
import React, {useEffect, useState} from 'react'

const ElectionCountdown = props =>
{
    const [electionTimer, setElectionTimer] = useState(null);

    useEffect(() => {
        if(props.electionStarted) fetchEndDateFromServer().then(r => setElectionTimer(new Date(r.data));
        else fetchStartDateFromServer().then(r => setElectionTimer(new Date(r.data));
    }, []);

    return (
        <Countdown date={electionTimer} />
    );
}*/
